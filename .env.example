# Copy this file to .env and fill the values. Do NOT commit .env.

# MongoDB root user (used by docker-compose)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password123rentalcar@

# App settings
PORT=3000
DATABASE_URL=
JWT_SECRET=your_jwt_secret_here

VERCEL_TOKEN=
